extends Node
class_name Graphs

#static func createLineGraph(data: Array) -> void:
	#var graphContainer: PackedScene = preload("res://addons/simulation/MusAPI/Basic/Scenes/graphContainer.tscn")
	#var graph = graphContainer.instantiate()
	#MusGroups.clientSpace.add_child(graph)
	#
	#var series = LineSeries.new()
	#that
	#graph.graph2D.add_series(series)
	#for i in data:
		#graph.graph2D.add_point()
